$('#like-icon-post-<%= @post.id.to_s %>').
  html('<%= link_to image_tag("liked-parts.png", size: "30x30"), group_post_like_path(@group, @post, @like), method: :DELETE, remote: true %>');
$('#like-text-post-<%= @post.id.to_s %>').
  html('<%= j render "posts/like_text", { likes: @post.likes } %>');