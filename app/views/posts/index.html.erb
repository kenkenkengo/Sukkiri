<% provide(:title, @group.name) %>
<div class="wrapper">
  <h3>グループ名：<%= @group.name %></h3>
  <%= render "main_post", group: @group, post: @post %>
  <%= will_paginate @posts, previous_label: '<', next_label: '>' %>
</div>
